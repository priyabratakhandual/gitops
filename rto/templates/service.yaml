apiVersion: v1
kind: Service
metadata:
  name: {{ include "rto.fullname" . }}
  labels:
    {{- include "rto.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rto.selectorLabels" . | nindent 4 }}
