apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rto.fullname" . }}-test-connection"
  labels:
    {{- include "rto.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rto.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
