apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deal.fullname" . }}-test-connection"
  labels:
    {{- include "deal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
