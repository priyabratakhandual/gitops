apiVersion: v1
kind: Service
metadata:
  name: {{ include "maturity-app.fullname" . }}
  labels:
    {{- include "maturity-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "maturity-app.selectorLabels" . | nindent 4 }}
